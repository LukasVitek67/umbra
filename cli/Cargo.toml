# SPDX-License-Identifier: AGPL-3.0-or-later
[package]
name = "nullchat-cli"
version = "0.0.1"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Runnable demo/CLI exercising the NullChat core on one machine."

[dependencies]
nullchat-core = { path = "../core" }
getrandom = "0.2"

[[bin]]
name = "nullchat"
path = "src/main.rs"

# Release signing: the key that decides which builds users will install.
[[bin]]
name = "nullchat-sign"
path = "src/bin/sign.rs"
