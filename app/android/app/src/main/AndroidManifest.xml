<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Everything leaves the phone through Tor; nothing else is needed. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- So the messenger can stay reachable while the screen is off. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- From API 34 a foreground service must also declare its type. Ours is
         "specialUse": the app holds a Tor onion service open so contacts can
         reach it, which is not any of the named categories. It is deliberately
         not "dataSync" — from Android 15 that type is capped at a few hours a
         day, and a messenger that stops being reachable after lunch is not a
         messenger. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Only to start again after a restart, and only when the user asked for
         it. See StayOnlineService and the switch in Settings. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="NullChat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:extractNativeLibs="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Keeps the process alive when the app is not on screen. Without it
             Android reclaims NullChat the moment you leave it: Tor stops, the
             onion service goes away, and nothing queued goes out until you open
             the app again. That is the single biggest reason a message "never
             arrived". -->
        <service
            android:name=".StayOnlineService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps a Tor onion service open so contacts can deliver messages peer-to-peer; there is no server to hold them." />
        </service>

        <!-- Starts the service again after a restart. Does nothing unless the
             user turned that on — see BootReceiver. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
